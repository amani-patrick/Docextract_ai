# DocuExtract AI  Dependencies
# Install: pip install -r requirements.txt

# ── API Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1

# ── OCR Engines ────────────────────────────────────────────────────────────────
paddlepaddle==2.6.2          # PaddleOCR backend (CPU build)
paddleocr==2.7.3             # Primary OCR engine
pytesseract==0.3.10          # Fallback OCR engine
Pillow==10.3.0               # Image processing

# ── PDF Support ────────────────────────────────────────────────────────────────
pdf2image==1.17.0            # PDF → images via poppler
PyMuPDF==1.24.2              # Fallback PDF handler (no poppler needed)

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy==1.26.4                # Array operations for image preprocessing
python-jose[cryptography]    # JWT (for future auth)
httpx==0.27.0                # Async HTTP client (for webhook callbacks)
tenacity==8.3.0              # Retry logic

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                # Test client
pytest-cov==5.0.0
